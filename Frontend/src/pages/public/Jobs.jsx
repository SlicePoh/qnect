import React from 'react'
import { layout } from '../../style'

export const Jobs = () => {
  return (
    <div className={`${layout.hero} w-full h-screen`}>
      No content yet....
    </div>
  )
}
